[project]
name = "api-nothink"
version = "1.0.0"
description = "OpenAI-compatible alias proxy with optional reasoning_effort injection"
readme = "README.md"
requires-python = ">=3.11"
dependencies = [
  "fastapi>=0.115,<1.0",
  "httpx[http2]>=0.28,<1.0",
  "uvicorn>=0.34,<1.0",
]

[dependency-groups]
dev = []
